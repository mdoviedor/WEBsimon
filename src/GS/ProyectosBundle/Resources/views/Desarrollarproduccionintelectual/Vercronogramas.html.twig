<table class="table table-hover table-condensed">
    <thead>

    </thead>
    <tbody>
{%for cronograma in cronogramas %}
{%if (cronograma.diferencia < 20) and (cronograma.diferencia >0)%}
        <tr>
            <td>
                <p><a href="{{ path('gs_proyectos_desarrollarproduccionintelectual_vista',{'id':tema.idtema}) }}">{{ cronograma.hito }}</a> Quedan {{cronograma.diferencia}} días para que se cumpla la fecha estimada.</p>
                <p><strong>Tema:</strong> {{ tema.titulo }}</p>
{{cronograma.descripcion}}
                <div class="progress progress-striped active">
                    <div class="progress-bar progress-bar-danger"  role="progressbar" aria-valuenow="45" aria-valuemin="0" aria-valuemax="100" style="width: {{cronograma.porcentaje}}%">
                        <span class="sr-only">45% Complete</span>
                    </div>
                </div>
            </td>
        </tr>
{%endif%}
{% endfor %}
    </tbody>
</table>
